server:
  port: 8080

spring:
  thymeleaf:
    cache: false

  application:
    name: oauth2

  datasource:
    url: jdbc:mariadb://localhost:3306/wearther
    username: root
    password: seoho123
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
